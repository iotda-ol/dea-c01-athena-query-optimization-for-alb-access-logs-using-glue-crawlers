# Common/Shared Module
# Provides reusable components across cloud providers

terraform {
  required_version = ">= 1.0"
}

# This module contains shared utilities and configurations
# that can be used across AWS, GCP, and Azure modules
